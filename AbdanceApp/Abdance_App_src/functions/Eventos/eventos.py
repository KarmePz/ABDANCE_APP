def eventos(request):
    #todo
    #get eventos y get evento por id
    #crea evento 
    #modifica datos de un evento existente
    #eliminar evento:
    #   eliminar todas las entradas asociadas al evento
    
    return 'hola eventos', 200


def evento_entrada(request):
    #todo 
    #-get all entradas separadas por eventos, get entrada por id
    # crea entrada y asignarle un id
    # la entrada no se puede eliminar
    # eliminar todas las entradas de un evento
    return